{
  "name": "diffyne-docs",
  "version": "1.0.0",
  "description": "Diffyne Documentation Site",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "sync": "node scripts/sync-docs.js",
    "deploy": "npm run sync && npm run build"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.25"
  }
}

